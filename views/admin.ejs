<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/_head %>

  </head>

  <body>
    <h1>Stats</h1>
      <span class="stats">
        <% for (let obj of votes) { %>
          <% for (let item in obj) { %>
          <ul class="list-group">
           <li class="list-group-item"><%= obj[item] %></li>
          <% } %>
        <% } %>
      </ul>
        <!--   [ anonymous {
              title: 'title B',
              description: 'description B',
              points: 100,
              name: 'Voter B' },
            anonymous {
              title: 'title A',
              description: 'description A',
              points: 90,
              name: 'Voter A' },
            anonymous {
              title: 'title C',
              description: 'description C',
              points: 120,
              name: 'Voter C' } ]  -->

      </span><p>
      <form id="deletePoll" action="/poll/:id/delete" method="POST">
        <input type="submit" value="Delete Poll">

    <% include ./partials/_bootstrap %>
  </body>
</html>
